#!/usr/bin/env bash

set -e

FORMAT="$1"

isabelle latex -o "$FORMAT"
isabelle latex -o bbl
[ -f root.idx ] && "$ISABELLE_HOME/src/Doc/sedindex" root
isabelle latex -o "$FORMAT"
isabelle latex -o "$FORMAT"
