#
# $Id$
#
# Common part for Doc Makefiles
#

## settings

LATEX = latex
BIBTEX = bibtex
RAIL = rail
SEDINDEX = ../sedindex

GARBAGE = *.aux *.log *.toc *.idx *.rai *.rao *.bbl *.ind *.blg *.out
OUTPUT = *.dvi *.pdf *.ps


## actions

nothing:

clean:
	@rm -f $(GARBAGE)

mrproper:
	@rm -f $(OUTPUT) $(GARBAGE)

isabelle.eps:
	test -r $* || ln -s ../gfx/$* .

isabelle_hol.eps:
	test -r $* || ln -s ../gfx/$* .

isabelle_zf.eps:
	test -r $* || ln -s ../gfx/$* .
